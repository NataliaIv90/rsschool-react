import React from 'react';
import { SearchPage } from '../searchPage/SearchPage';

const App: React.FC = (): JSX.Element => {
  return (
    <div className="App">
      <SearchPage />
    </div>
  );
};

export default App;
